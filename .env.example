# Invoice QC Service Configuration
# Copy this file to .env and adjust values as needed

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Tolerance for floating-point comparisons in validation (default: 0.01)
AMOUNT_TOLERANCE=0.01

# Maximum file size for PDF uploads in MB (default: 10)
MAX_UPLOAD_SIZE_MB=10
